services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - node_modules:/home/vscode/app/node_modules
      - ../:/home/vscode/app:cached
    ports:
      - "4321:4321"
    tty: true
    stdin_open: true

volumes:
  node_modules:
